<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.FlappyBird" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Main Theme extending Base -->
    <style name="Theme.FlappyBird" parent="Base.Theme.FlappyBird" />

    <!-- Define AppTheme to avoid errors -->
    <style name="AppTheme" parent="Theme.FlappyBird" />

    <!-- NoActionBar Theme -->
    <style name="AppTheme.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>
</resources>
